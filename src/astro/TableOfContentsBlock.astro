---
import { blockValueToOverrides } from "../config.js";
import TableOfContents from "./TableOfContents.astro";

const overrides = blockValueToOverrides(Astro.props.value ?? {});
---

<TableOfContents {...overrides} />

